<h1>Main page</h1>
<h2>All posts</h2>
<% @post.each do |post| %>
<div class="card">
  <h3><%= post.title %></h3>
  <p><%= post.body %></p>
  <%= link_to 'Read more', post_path(post), :class => 'btn btn-outline-primary' %>
</div>
<br/>
<% end %>
